def solution(s):
    a = ""
    if len(s) % 2 == 0:
        a = s[int(len(s)/2) - 1] + s[int(len(s)/2)]
    else:
        a = s[int(len(s)/2)]
    return a


--------------------------------------- or

def string_middle(str):

    return str[(len(str)-1)//2:len(str)//2+1]
