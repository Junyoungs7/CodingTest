for test_case in range(1, T + 1):
    n = list(input())
    count = 0
    for i in range(len(n)):
        if n[i] == n[-i-1]:
            count += 1      
    if count == len(n):
        print(f"#{test_case} 1")
    else:
        print(f"#{test_case} 0")
